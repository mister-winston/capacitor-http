{
  "name": "http-example",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@capacitor-community/electron": "^5.0.1",
    "@capacitor-community/http": "file:..",
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "@types/node": "^22.13.0"
  },
  "scripts": {
    "build-ui": "cd www && npm run build",
    "open-desktop": "npm run build-ui && cap sync @capacitor-community/electron && cap open @capacitor-community/electron",
    "open-android": "npm run build-ui && cap sync android && cap open android",
    "open-ios": "npm run build-ui && cap sync ios && cap open ios"
  }
}
